apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-argocd-staging
  namespace: argocd
  labels:
    app: hello-world
    environment: staging
    essesseff: "true"
spec:
  project: default
  source:
    repoURL: https://github.com/essesseff-hello-world-nodejs-template/hello-world-argocd-staging
    targetRevision: main
    path: argocd
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

